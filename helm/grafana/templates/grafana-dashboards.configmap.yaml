apiVersion: v1
kind: ConfigMap
metadata:
  annotations:
  name: grafana-dashboards
  namespace: {{ .Values.namespace }}
data:
  pods.dashboard.json: {{ index .Values "grafana" "dashboards" "pods" | quote }}